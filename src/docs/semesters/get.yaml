GET semesters
---
tags:
  - Semesters

parameters:
  - in: header
    name: Authorization
    required: false

responses:
  200:
    description: List of semesters

  401:
    description: Fails to get items due to authentication error